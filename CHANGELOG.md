# Changelog

本项目遵循语义化版本；日期使用 YYYY-MM-DD。

## [0.6.1] - 2026-09-09

- 修复真实桌面验收发现的语义关联错位：用静态扫描位置验证模型声称的关联编号；不匹配或原文定位不唯一时单独保留语义建议，避免将平台证据附到发布日期问题。
- 请求中的候选显式提供从1开始的编号，补充相邻错号、重复原句、宽上下文和部分有效关联回归。
- 补录第三次真实桌面审查：28.0秒、5次模型请求、实际读取两份参考文件；逐条确认、GitHub风格diff和原生导出通过。原文与LF换行均保持，未将本次结果冒称为0.6.1在线复测。

## [0.6.0] - 2026-09-09

- 修改建议使用GitHub风格diff，带原/新行号、增删符号和字词高亮；支持可信Unicode位置，避免改错重复片段。
- 长文分段审查可按批续查，保留已有用户决定并明确覆盖范围；正文或配置变化后不续用旧会话。
- 工作区按页读取并提供长行续页游标，搜索按实际覆盖范围标注截断，文件读取前后均限制大小。
- 增加后台审查任务、实时进度、工具调用与结果、公开决策摘要，以及取消和重新审查。
- 连续输出按模型请求合并显示，阅读历史工具结果时不强制滚动；格式不合格时最多修复一次，并显示所在通道和安全诊断。
- 工作区 Agent 支持受限的列目录、搜索、按行读取，带轮次、响应大小和时间预算；DeepSeek V4 使用低延迟和 JSON 模式。
- 工作区查证不能仅列目录就结束，必须请求正文搜索或读取；demo 文件固定 LF，跨平台检出保持快照一致。
- 存在参考文件时，重读原稿不算完成查证；关联语义补充保留引用与理由，冲突留给作者核实。另存为保留待保存文本的换行，默认窗口适配较小屏幕。
- 所有改写默认待确认，仅采纳内容进入修改稿；重叠改写降为待确认，避免整份审查失败。
- 增加无需密钥的离线桌面/浏览器演示、可重复性能门禁和真实模型知识查证记录。
- 增加 Windows 便携目录包、macOS 两种架构原生构建、校验和及依赖许可清单。
- 增加上海开源大赛项目材料及独立首次使用者体验记录。

## [0.5.0] - 2026-09-08

### Added

- 修改稿逐条展示修改原因、原文上下文，以及撤销和恢复操作。
- 全文核对页支持在各处变更间导航，并实时反映撤销结果。
- 桌面版区分“保存”和“另存为”；覆盖原文件前明确确认并校验打开时内容哈希。
- 可重复验证的桌面演示包，包含待审查文档、工作区、事实依据、结果快照和参考修改稿。

## [0.4.0] - 2026-09-08

### Added

- 两路并发语义复核：分别裁决静态候选和独立发现规则未覆盖的问题。
- 可感知的语义复核状态、完成时间、耗时、调用数和模型新发现标识。
- 桌面工作区浏览、受控文件读取及带哈希校验的确认写回。

### Changed

- 修改稿完全由本地代码根据 REWRITE 决策生成，不再信任模型提供的完整稿件。

## [0.3.0] - 2026-09-08

### Added

- Vue 3 + pywebview 桌面工作台和 Windows 单文件 EXE 构建流程。
- OpenAI-compatible 内置 Agent、KEEP / REWRITE / VERIFY / ASK 输出契约。
- 发送前确认、内存密钥、修改稿预览与显式另存为安全边界。
- Windows 构建 artifact 与打包后 smoke test。

### Changed

- Windows CI 使用仓库内 pytest 临时目录和 UTF-8 环境，避免系统临时目录权限与编码差异。

## [0.2.0] - 2026-09-08

- 增加讨论/纠偏过程痕迹及上下文依赖措辞检查。
- 扩展 Markdown、飞书文档覆盖和反馈记录能力。
