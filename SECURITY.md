# 安全政策

## 支持范围

安全修复优先作用于最新的 `main` 分支和最新发布版本。

## 报告漏洞

请使用 GitHub Security Advisories 的私密报告入口，不要在公开 Issue 中粘贴 API Key、未公开文档、访问令牌或可利用细节。报告应包含影响版本、复现条件、预期影响和建议修复（如有）。

## 桌面版数据边界

- 本地静态检查不联网。
- API Key 仅用于用户确认后的模型请求，不持久化、不写日志。
- Agent 请求会把当前正文与静态发现发送到用户填写的 Base URL。
- 模型返回内容按固定 JSON 契约验证；空修改稿或非法动作会被拒绝。
- 原文件不会自动覆盖，保存路径由用户明确选择。

用户应只配置其信任且允许处理相应文档的模型服务。
