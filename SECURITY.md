# 安全政策

## 支持范围

安全修复优先作用于最新的 `main` 分支和最新发布版本。

## 报告漏洞

请使用 GitHub Security Advisories 的私密报告入口，不要在公开 Issue 中粘贴 API Key、未公开文档、访问令牌或可利用细节。报告应包含影响版本、复现条件、预期影响和建议修复（如有）。

## 桌面版数据边界

- 本地静态检查不联网。
- API Key 仅用于用户确认后的模型请求，不持久化、不写日志。
- Agent 请求会把当前正文与静态发现发送到用户填写的 Base URL。工作区模式还会发送所选目录内受支持文本文件的相对路径索引，但不会自动发送其他文件正文。
- 候选裁决和全文独立发现并发执行；模型返回内容按固定 JSON 契约验证，修改稿仅由本地代码根据通过校验的 REWRITE 决策生成。
- 工作区访问限定在用户主动选择的根目录内，拒绝路径越界和符号链接，并忽略常见依赖、版本库与构建目录。
- 原文件不会自动覆盖。“保存”和工作区写回均需要用户再次确认，并使用打开文件时的内容哈希阻止覆盖外部修改；“另存为”由用户通过系统文件对话框选择新路径。

用户应只配置其信任且允许处理相应文档的模型服务。
